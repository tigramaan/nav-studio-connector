# System specification

The normative feature requirements are in `001-desktop-connector/spec.md`. The system consists of a pure trust-policy domain, application workflows, network and platform adapters, a JSON CLI and a Tauri GUI. The LAN and all discovery records are untrusted input. Trust changes require REQ-004 or REQ-005 authorization.
